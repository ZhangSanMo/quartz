# SSH框架
SSH通常指的是用Struts2做前端控制器，Spring管理各层的组件，Hiberate负责持久层。

# SSM框架
SSM通常指的是SpringMVC做前端控制器，Spring管理各层的组件，MyBatis负责持久化层。

# Spring WebFlux

对比SpringMVC构建于Servelet API和Servlet 容器，是一个同步阻塞式I/O框架，就是一个线程处理一个请求。

Spring WebFlux基于Reactive Streams API和Servlet 3.1+容器，是一个异步非阻塞的框架，能够充分利用多核CPU的硬件资源去处理大量的并发请求。